---
title: "Local Inference Sketch"
date: "2026-06-24"
description: "A placeholder post that exercises Python, bash, JSON, and image rendering."
tags:
  - ml
  - infrastructure
---

This second seed post gives the build a little more surface area: several
highlighted languages, another image, and a small JSON block.

![Inference placeholder](/images/blog/local-inference.svg)

```python
def tokens_per_second(tokens: int, seconds: float) -> float:
    return tokens / seconds
```

```bash
pnpm build
```

```json
{
  "runtime": "local",
  "languages": ["python", "typescript", "javascript", "bash", "json"]
}
```

Build-time highlighting should leave static markup in the page, with no browser
highlighter runtime attached.[^runtime]

[^runtime]: This is a plumbing fixture, not a benchmark claim.
