# Handoff — beneverman.com (2026-07-09, evening)

For the next agent (Fable). Ingest, act, then delete this file — durable
knowledge lives in `PRODUCT.md` / `PROMPT.md` / the Linear project, not here.

## Working model (unchanged, important)

- **Claude/Fable** = planning, specs, and the **interactive blog design** with
  Ben — screenshot-driven, one decision at a time. **Codex** = mechanical
  tickets. The blog's look is never ticketed to Codex.
- Branch `codex/bcp-2826-next-mdx-blog`, single working branch, **commit as you
  go, no PR ceremony until prod**. Dev server usually already running on
  http://localhost:3000 (check before starting another).

## Where to pick up

1. **Get Ben's reaction to the Callout/summary-card design** — v1 is live at
   `/blog/neural-nets-for-eye-disease` (top of page): quiet bordered aside,
   small mono uppercase label. Ben likes the *idea* of a summary card but
   explicitly not the old site's styling; he had not yet seen/judged v1 when
   the session ended. Footnote treatment (GFM superscripts + generated
   "Footnotes" section) also awaits his reaction — renaming to "References"
   or quieting the heading are easy follow-ups.
2. **Continue the section-by-section port** of the eye-disease post (Ben's
   chosen process: port one section at a time, decide components as they come
   up). Section 1 ("Doctors Can't Keep Up") is done. Remaining, in order:
   - "Using Math to See Disease" — first images. Copy the 3 pngs from
     `web/beneverman.com-v6-fumadocs/public/blog/neural-nets-for-eye-disease/`
     into `public/images/blog/neural-nets-for-eye-disease/` and decide the
     figure/caption treatment (source uses a stray inline-code line as caption).
   - "Implementing a CNN…" — has the 3/31/24 update `<Callout type="warn">`;
     decide whether Callout grows type variants (warn/error) or stays one look.
   - "The Code, Explained" — mostly code blocks (already styled); two more
     callouts, one `type='error'` GPU warning.
   - Final Thoughts — `<Email />` → plain `mailto:ben@beneverman.com` link
     (decided). Also `$t$` math on one line → italic *t* (no math plugin).
3. **Still pending, decided but not done**: delete the two placeholder posts
   (`agent-workshop-notes`, `local-inference-sketch`).

## Port decisions already made (don't relitigate)

- Source of truth: **local** `~/Documents/Code/web/beneverman.com-v6-fumadocs`
  (its `^[…]` inline footnotes convert to GFM `[^n]`). The sibling
  `web/beneverman.com-v6` is the older variant — its only lasting contribution
  is the original publish **date: 2024-02-28**, which is what our frontmatter
  uses.
- The `minimalist-ai-agent` post is **not** being ported (Ben's call).
- Component map lives in `lib/blog.ts` (`mdxComponents`), components in
  `components/mdx/`. First entry: `Callout` (`label` prop).

## Session gotchas (learned the hard way)

- **Never repeatedly `open -a "Google Chrome" <url>`** — it hijacks Ben's
  browser and he hates it. Ben has now connected the **Claude-in-Chrome
  extension** (`/chrome`): use `mcp__claude-in-chrome__*` tools for
  navigation/scroll/DOM in tabs it opens itself. Computer-use MCP sees Chrome
  screenshots but browsers are read-tier (no clicks/typing/scroll).
- `safe` blocks directory deletion entirely — hand directory cleanup to Ben
  instead of attempting it. A stray clone at
  `~/Documents/Code/beneverman.com-v6-fumadocs` awaits his trash (identical to
  the `web/` copy).
- `safe git commit` requires explicit paths (`-- <paths>`); plain `git rm` +
  commit without paths is blocked.
